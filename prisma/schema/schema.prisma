// FAZIA Housing — Prisma root
// Multi-file schema. Architecture frozen — Module 0 baselines only.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
