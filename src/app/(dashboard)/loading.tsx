import { PageSkeleton } from "@/components/feedback";

/** Instant soft-nav feedback while the destination RSC stream resolves. */
export default function DashboardLoading() {
  return <PageSkeleton />;
}
